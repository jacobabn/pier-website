{
  "name": "pier-website",
  "version": "1.0.0",
  "private": true,
  "description": "Pier — marketing site (Drydock direction). Static HTML + pre-compiled Tailwind, deployed on Vercel.",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./styles.css --minify",
    "dev": "tailwindcss -i ./src/input.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
